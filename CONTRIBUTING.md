# Contributing

Groky is in its initial development phase.

1. Branch from `develop`.
2. Keep each change focused and include verification steps.
3. Run `pnpm build` and the Rust checks documented in `AGENTS.md`.
4. Open pull requests against `develop`.

Please do not commit credentials, Grok session data, `.env` files, or captured ACP traffic.
